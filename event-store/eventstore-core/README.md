# eventstore-core

Core traits, shared types, and errors for the Event Store. Also re-exports protobuf-generated types used across crates.

- Defines backend trait(s) and service interfaces
- Shared domain types (events, streams, versions, positions)
- Error types and helpers

See `src/lib.rs` for details.
