# SDKs

- `sdk-ts`: TypeScript SDK (runtime client + ts-proto stubs via `make gen-ts`).
- `sdk-py`: Python SDK (grpcio; generate with `make gen-py`).
- `sdk-rs`: Rust SDK (uses `eventstore-proto` crate).

Each SDK contains a basic example for append/read.
