//! Event store client integration

/// Event store client for communicating with the gRPC API
pub struct EventStoreClient {
    // TODO: Add tonic client
}

impl EventStoreClient {
    /// Create a new event store client
    pub fn new(_address: String) -> Self {
        // TODO: Initialize tonic client
        Self {}
    }
}
